import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderRadius: 30,
    textAlign: "center",
    paddingVertical: 15,
    borderColor: "black",
  },
});

export default styles;
