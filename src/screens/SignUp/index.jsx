import { View, Text } from "react-native";

const SignUp = () => {
    console.log(process.env.firebase)
  return (
    <View>
      <Text>I'm the auth screen</Text>
    </View>
  );
};

export default SignUp;
