import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  backBtn: { position: "absolute", top: 40, right: 25 },
  generalHorizontalPadding: { paddingHorizontal: 8 },
});

export default commonStyles;
